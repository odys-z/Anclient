#pragma once

#define menu_home (string{ui->global<IDs>().get_m_home()})
#define menu_volume (string{ui->global<IDs>().get_m_volume()})
#define menu_album (string{ui->global<IDs>().get_m_album()})
#define menu_settings (string{ui->global<IDs>().get_m_settings()})

#define menu_user (string{ui->global<IDs>().get_m_user()})
#define menu_edge (string{ui->global<IDs>().get_m_edge()})

