export namespace regex {
    function sharp_(str: string, defltStr: string): string;
    function desharp_(str: string): string;
    const _regImage: RegExp;
    function mime2type(mime: string): string;
    function type2mime(doctype: any): string;
}
